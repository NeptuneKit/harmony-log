# 2026-04-02

## 决策记录
- 为统一仓库文档名、导入名与发布名，OHPM 发布包名由 `neptunekit-harmony-log` 统一为 `harmony-log`。
- 已调整发布脚本 `scripts/ci/build-temp-har.sh`，后续 CI 产物 `library/oh-package.json5` 的 `name` 将为 `harmony-log`。
- README 安装命令同步改为 `ohpm install harmony-log` / `ohpm i harmony-log`。

## 风险与备注
- `ohpm info harmony-log` 当前返回 404（未在公开仓库查询到），需在实际发布时再次确认命名策略和组织账号发布权限。
- 当前仓库现有 Node 测试存在历史失败（与本次命名改动无直接关系），本次仅做包名与文档一致性修复。

## 发布里程碑
- 2026-04-02 已执行 OHPM 发布验证：
  - Dry Run 成功：GitHub Actions run `23880202233`。
  - 正式发布成功：GitHub Actions run `23880259199`。
- 发布日志显示：`package:harmony-log@1.0.5`、`+harmony-log 1.0.5`。
- 备注：本地立即执行 `ohpm info harmony-log` 仍返回 404，可能为索引延迟或可见性策略导致，建议稍后再次查询确认。
- 审核修复：补齐 `CHANGELOG.md` 的 `1.0.4`、`1.0.5` 版本条目，满足“当前发布版本必须在变更日志中出现”的审核要求。
- 偏好更新：用户要求“每次改动都要打包并将产物上传到仓库（提交 dist）”，后续流程固定执行。
- 发布流程策略更新：每次 `main` 提交自动创建语义化 tag；每个 tag 触发 OHPM 发布；发布成功后自动创建/更新 GitHub Release。
